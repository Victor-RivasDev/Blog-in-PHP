<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirige todo el tráfico entrante hacia la carpeta /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>